# 校园 Demo 实施路线（当前执行基线）

更新：2026-09-05。用户已确认后续按本文件推进。本文取代旧 To Do 的执行顺序，旧编号仅用于追溯，不表示必须重做旧功能。

## 1. 产品与工程边界

- 核心是自主社会：200 名持久 NPC、约 20 个动态 LLM 槽位、最多 6 名永久觉醒角色；约 6000 人是人口背景，不逐人模拟。
- 28 天、4 个阶段、每阶段 7 天；上午/下午/晚上/凌晨；聊天、购物、吃饭、普通移动不扣主要行动。夜间可以留在表世界。
- 玩家与 NPC 共用权威状态、地点、资源、任务条件和行动预算。表里世界不是两份人物存档。
- 神经模型只提出方案、选择合法行动和表达，不创造事实、物品、奖励或伤害。玩家聊天不设硬次数限制；开发测试预算与后台思考预算分别管理。
- Godot 负责输入、场景与 UI；Python 负责模拟和结算；Mac 开发，Windows 发布。保留旧工程与用户素材，不合并 main。
- 当前场景以 `Project-c-0.1(1).rar` 中七张 PNG 为准：校门、生活区、东宿舍、西宿舍、心理学院与桥、图书馆、运动场。详见 `ART_COLLABORATION_INTEGRATION.md`。
- UI 先可用再美化，但不能最后才接。公开游戏界面用于布局参考，可复用素材必须有明确许可。

## 2. 2026-09-05 进度审计

代码基线：分支 `codex/campus-demo-architecture`，本轮开始 HEAD `451b187`。以下是代码接入状态，不将历史测试记录当作本轮实测。

| 范围 | 当前证据与状态 | 尚需补齐 |
| --- | --- | --- |
| 权威内核 | `WorldKernel`、事务、事件、RNG、检查点、契约已有 | 玩家存读档界面、迁移、发布包恢复仍需贯通 |
| 物品经济 | 旧 `/trade`、`/use-item`、`/action` 操作旧 `SimulationBridge.world`；校园运行在独立 `CampusKernelBridge.kernel.state` | 不能宣称校园库存已接通；优先迁移到校园事务，不做双写同步 |
| 地图人口 | 地点图、200 NPC、活动、路线与可见人物已接入 | 本轮七图基线；精确碰撞、遮挡、楼梯和室内可见性仍属后续工作 |
| 社会系统 | 社团、关系、论坛、日志、手机、提议与队伍有运行代码 | 不等于完整恋爱、长线目标、背景人口经济或持续局势已经完成 |
| LLM | 通用兼容接口、事实边界、记忆、候选、降级已有 | 合法多步计划、计划修订和真实后果评测；不以微调为前置条件 |
| 夜相 | 进入资格、污染、月相、任务已接入 | 当前 NPC 晚上接单、凌晨执行仍是简化版，需要自主错峰浏览和真实目标执行 |
| 卡牌战斗 | 部署、抽弃牌、费用、目标、基础效果、胜利接口已有 | `end_combat_round` 尚跳过真实敌方行动；持续状态、撤退、败北、后台战斗缺失 |
| 调查成长主线 | 有设定、内容与部分领域结构 | 尚无完整证据→理解→洞察→干预→阶段结局闭环 |

旧清单进度是第 16 步第三子阶段；按新路线，应先回补第 1～3 步，再继续第 4 步，不能把旧清单的“已完成”直接换算为整体完成率。

## 3. 唯一实施顺序与验收条件

| 步骤 | 工作内容 | 依赖与完成条件 | 当前状态 |
| --- | --- | --- | --- |
| 1 | 统一规则、进度与七图基线 | 文档无现行版本冲突；七图目录、图片尺寸、双向通行与区域投影有测试 | 已完成本轮回补与验收 |
| 2 | 打通新旧物品经济及角色状态 | 校园买药→转交→使用/装备→实际状态→存读档；全部由校园事务执行，旧版回归不变 | 进行中：校园库存、商店与六行动首版已接通；治疗与自主私人交易待补 |
| 3 | 统一功能 UI 和可替换主题 | HUD、手机、人物、背包、交易、队伍、提示、设置入口一致；显示事实和真实可用状态 | 有原型，待统一 |
| 4 | 卡牌战斗完整闭环 | 敌方意图/行动、状态、污染、倒下、撤退/败北/胜利、消耗回写；依赖 2～3 | 部分已有 |
| 5 | 调查与证据 | 观察、搜查、询问、来源、可信度、关联、分享/隐瞒；玩家与 NPC 同规则；调查笔记 UI | 待实现 |
| 6 | 知识与成长 | 阅读/课程/案例/应用/反思、属性、能力、牌组配置；知识打开实际行动和洞察区，不只加伤害 | 待贯通，依赖 4～5 |
| 7 | NPC 长期目标与计划 | 持续目标、准备物资、求助、协作、受阻改计划；结构化计划每步重新校验 | 基础决策已有，依赖 2、5 |
| 8 | NPC 自主办事 | 发单、错峰浏览、原子锁、交易备战、集合、执行、同规则后台战斗、失败与结算 | 基础已有，依赖 4、7 |
| 9 | 持续局势与因果链 | 资源短缺、纠纷、失踪、污染等局势演化；行为改变后续任务、地点和人物；双论坛与年表可追溯 | 基础任务链已有，待扩展 |
| 10 | 失心者多解法 | 外壳、心结、异常自洽度、证据洞察；白天支持/夜间净化/混合路线及不同代价 | 依赖 5～9 |
| 11 | 七天完整样本 | 同一事件分别测试玩家介入、完全不介入、关闭 API；都有可理解且可追溯结果 | 内容扩量前硬门槛 |
| 12 | 校园生活内容补齐 | 课程、社团晋升、交友约会、兼职购物、医疗安保、比赛节庆；以资源/关系/机会区分玩法 | 不重做已有社团底座 |
| 13 | 四阶段主线与结局 | 导师书单、遗忘与身份谜团、DDL、动态参与者、替代线索与失败推进；从第 1 天到 28 天 | 依赖 11；主线事实不由 LLM 改写 |
| 14 | 场景与表现完整整合 | 七图精确通行、楼梯、遮挡、室内交互、表里变化、角色动作及战斗反馈；与美术协作 | 基础素材现在接，精细化后做 |
| 15 | 完整体验与稳定性 | 引导、存档迁移、服务断线恢复、性能、内容节奏、长期日志、28 天多种子回归与 LLM 对照评测 | 全程持续检查，末期总验收 |
| 16 | Windows 发布 | 打包本地服务、干净机器安装/启动、API 设置、存档与路径、错误日志、许可清单 | 现在做冒烟，末期完整验收 |

每步必须有玩家可操作的界面与结果反馈。规则、契约、测试、运行时和 UI 是同一功能的交付物，不能只创建结构就勾选完成。

## 4. 前序补齐清单（按此顺序开展）

### 1A. 本轮基线修正

- 固定本文件为执行入口，旧 To Do 改为历史索引；修正战斗文档过期状态和旧架构实施时间表的适用范围。
- 七图按源包逐文件 SHA-256 对照。原五图相同则不重复替换；新增图书馆 V3、运动场 V2。
- 新图复用已有 `central_region`、`sports_health_region`，不删除未获得美术的其他逻辑区域和室内节点。
- 通过已有道路接入双向步行网络；地图按钮仍走权威快速移动，不直接修改位置。
- 将中央区、体育区从其他图片的可见区域中移出，避免已有独立图的 NPC 被借到别的区域显示。
- 仅有平面图片时，不声称建筑碰撞、室内遮挡和精确地面寻路已经完整。

### 2A. 校园物品经济迁移

1. 明确校园 `population` 中现有 `wealth` 为角色金钱权威，`inventories` 为库存权威；用稳定 ID 关联，不再新增并行钱包余额。
2. 复用既有物品定义与纯结算规则，增加校园内容映射、初始库存和商店。不要把旧世界全部嵌入 metadata，也不要同步维护两份金钱。
3. 在校园命令中接入买卖与六项物品行动。实际伤势/污染/能力未具备时明确拒绝，不用假状态宣称完成。
4. 接入 NPC 保留数量、职业工具、同物品冷却、报价记忆和原子报价结算；免费交易仍受需求与行为频率约束。
5. Godot 新校园背包/交易只读校园投影；旧调试场景继续使用旧接口。
6. 验证资源守恒、失败回滚、请求幂等、存档续接、NPC 多日交易及新旧全局回归。

#### 2A 第一批实现（2026-09-05）

- `campus_inventory.py` 接入校园事务：201 份角色库存、原有唯一 `wealth` 账户、三处商店、九种校园名称物品。旧版 36 种物品与旧世界不修改。
- 超市卖场、医院药房、食堂窗口按逻辑地点和营业时段结算；库存有限、余额有限、服务端定价、收款交货同事务。初始价格/库存是验证参数，尚未做经济平衡。
- 买卖及六行动统一命令已注册。食物消耗库存并降低现有 `needs.food`；装备只登记真实部位，**尚不产生战斗防护加成**。医疗、伪装、调查用品明确返回 `requires_other_system`，不消耗或制造假效果。
- 转交须同地同层；地面物品按表/里世界及逻辑地点隔离。装备需先卸下才能转移，医生/维修人员职业用品保留最后一份。
- 手机商城可查看价格、库存、负重、钱包，选择物品行动与现场接收者；到柜台按钮逐段调用现有通行接口，不远程取货。战斗中关闭生活物品操作，避免绕过战斗费用。
- 校园视图 v23 新增可选 `economy` 字段（向后兼容增量）；契约见 `contracts/campus_item_command.schema.json`。检查点保存/恢复并校验新库存；不重复初始化补货。**玩家存读档界面及旧校园检查点迁移仍未完成。**
- 已通过物品专项 22 项、全局 390 项、旧版 46 项；Godot 物品流程 headless/图形均通过，实际鼠标购买确认扣款入库且不扣时段。其他回归详见 `production/VERIFICATION.md`。

#### 新前置规则与接续顺序（第 2 步仍不能勾选完成）

1. **待用户确认：**生命值及伤势是否跨战斗保留到校园生活？建议保留，由药品/医院恢复；当前只有战斗内生命值，绷带不能据此宣称治疗校园角色。
2. 确认后补齐统一身体状态、治疗目标与适应状态、消耗和结果；再接战斗出入时的权威读写。不同时维护独立生活 HP 和战斗 HP。
3. 继续 NPC 必需品保留、同物品冷却、最近价格与报价记忆、需求/关系/经济压力、自主采购与报价接受/拒绝/原子结算。**本批仅有共用行动和职业保护，不声称 NPC 已自主交易。**
4. 七至十四日库存/交易/资源守恒模拟、检查点续接及 Godot NPC 交易可见反馈通过后，才完成第 2 步并进入第 3 步。

### 3A. 共用 UI 与跨系统约束

- 共享 Theme、字体与控件；先复用现有素材和 Godot 控件，后续可引入许可明确的公开 UI 包。
- 区分未实现、未解锁、条件不足、请求中、成功和失败，不能用看似可点的占位按钮误导玩家。
- 战斗胜利只是任务目标进展，不应让所有调查/护送/救援任务自动完成；任务按各自目标条件结算。
- 战斗的世界行动成本必须与普通活动一致，不能通过准备/换队/重开刷免费战斗。
- 新地图的具体室内和路径落位随步骤 14 精化，保留现有逻辑地点与旧存档。

## 5. 验收和继续工作规则

1. 每个主要功能先做专项和全局 Python 测试，再做 Godot 导入、端到端与图形操作；有世界后果的功能加 7～14 日无人模拟。
2. 通过后更新本文件状态和 `production/VERIFICATION.md`，提交/上传当前 `codex/` 分支；不得自动合并 main 或删除旧工程。
3. 检查旧待推送提交也满足相同验收；失败先修复，不用跳过测试、强推或把未完成写为完成。
4. 不确定且会改变用户规则的设计先询问；发现新增前置条件先记录并补齐，不跨越依赖。
5. 世界记录要区分“想做”“接受了”“到达了”“实际完成了”。不能靠论坛文案制造未发生的涌现。
6. API 测试默认规则/模拟提供器，真实联网另设小预算。不得把密钥、私人配置、缓存、屏幕截图或原始聊天写入提交。
7. Windows 实机未测时明确写未测；Mac 图形测试不能替代 Windows 发布验收。

## 6. 参考依据（结构借鉴，不复制商业美术）

- [Persona 5 Royal](https://persona.atlus.com/p5r/index.html?lang=en)：日常、关系与夜间活动互相影响。
- [Shadows of Doubt](https://store.steampowered.com/app/986130/Shadows_of_Doubt/)：持久居民生活与可调查事实。
- [RimWorld](https://rimworldgame.com/)：需求、关系和持续后果共同产生故事。
- [Slay the Spire](https://www.megacrit.com/press-kits/slay-the-spire/)：卡牌信息与战斗反馈；不引入肉鸽结构。
- [Godot Theme](https://docs.godotengine.org/en/4.5/tutorials/ui/gui_skinning.html)、[Kenney UI Pack（CC0）](https://kenney.nl/assets/ui-pack)：可替换界面框架；本轮没有下载第三方 UI。

## 7. 本轮交付与下一入口

- 第 1 步已完成：执行路线、进度审计、历史文档适用范围、七图接入、真实区域投影、切图旧移动回放清理与出口防连跳。
- Python 全局 368 项、旧版 46 项通过；末次 Godot 脚本修正后又通过 26 项源码/素材专项。全局包含校园七日无人推进测试。
- Godot 4.7.2 导入、组件、导航、卡牌回合测试通过；七图完整协作流程在 headless 和 Metal 图形模式均输出 `CAMPUS_COLLAB_FLOW_OK`。图形手动核对七图目录、图书馆与运动场地面显示。
- 通过 computer-use 操作真实 Godot 窗口，而非仅凭 PNG 文件认定场景可用；未进行 Windows 实机验收。本轮未调用付费 LLM。
- 当前停在第 2 步 / 2A 第一批之后：校园库存、商店与基本物品操作已接通，等待确认持续伤势规则。NPC 自主交易、完整功能 UI、精确建筑碰撞与后台战斗仍未完成，不勾选整步。
