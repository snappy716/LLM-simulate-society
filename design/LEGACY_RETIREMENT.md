# 旧城镇下线清单

2026-09-05 用户明确允许删除旧城镇，覆盖此前必须保留旧工程的约束。只删除旧城镇，不删除七张校园图、校园复用的人物/动画、通用 LLM 接口或用户私人世界观文件。

## 第一批：独立副本退出仓库

- 删除仓库中的 `emergent_town_demo/`、`project-a-0.2/`、`ProjectA-0.21.zip`，共 148 个已跟踪文件。删除前旧版 46 项测试通过；本机将原目录连同未跟踪/被忽略的运行数据一起移动到仓库外备份，避免把私人配置当作废弃代码清空。
- Git 历史保留这些文件，恢复参考删除前提交 `3f570ca`。不清洗 Git 历史，不强推，不合并 main。
- 移除三个依赖旧源文件的逐项等价对照测试；原文件中的三个仍有效的快照/Schema/迁移测试迁到 `test_architecture_contracts.py`，并新增旧副本不存在、校园入口存在的检查。不把测试数量下降当成功能减少，也不跳过校园测试。

## 第二批：校园独立启动（已通过验收）

- 正常服务不再创建旧 `World`；新进程导入并启动校园后，`simulation.runtime` 和 `simulation.api.legacy_bridge` 均不在已加载模块中。200 名校园 NPC 不变，没有偷偷运行第二套 200 人社会。
- 配置加载抽到 `simulation/settings.py`；包入口改为校园服务。旧桥接只留在 `simulation/api/legacy_bridge.py`，仅历史功能测试显式导入，不挂生产 HTTP。这是隔离，不是全量删除旧运行时。
- Godot 默认主场景改为七图校园，启动握手直接读取 `/kernel/campus-snapshot`。旧 `/snapshot`、`/step`、`/trade`、`/use-item`、`/action` 明确返回 410，不自动重建旧世界。
- 通用 API 设置成为共用常驻面板，可从校园手机打开；没有其他面板时 Esc 可打开/关闭。暂停、关闭恢复、遮挡时禁开其他面板有测试。配置不会自动联网，启动仍离线，不增加聊天次数限制。
- 保留工程的原用户数据标识，已有接口配置和手动槽位置不变；内容版本未变。实读改动前的校园存档，除运行版本单调递增外，完整状态和 RNG 与磁盘原档一致。
- 全局 481 项、专项 7 项、Godot 默认启动/设置与原有各流程、七日自然模拟通过，详见 `production/VERIFICATION.md`。Mac 实测，不代表 Windows 发布验收。

## 第三批：旧客户端与地图工具（已上传 1ff582c）

- 从工作目录移除 30 个旧城镇专属文件：旧人口显示、交易/行动 UI、全城地图 UI、廷根图与临时 Tile、蓝图生成器及对应场景/UID。仓库外已有逐文件备份；Git 恢复基线为 `6786b9d`。不处理私人配置和历史运行数据。
- Godot 桥移除旧快照、旧信号及 `/step`、`/trade`、`/use-item`、`/action` 请求。校园统一行动、配置、校园存档保持；启动空响应使用不刷引擎报错的解析方式。
- NPC 控制器、校园灰盒验收场景仍被校园复用，不能按名字误删。七图、人物素材、校园地图目录、全部 `content/` 与 Python 模拟均与本批开始时相同；内容哈希和存档 Schema 不变。
- 新增资源完整性测试，校验退役文件不存在、剩余静态 `res://` 引用可解析、旧请求不再暴露、七图和人物组件仍在。旧蓝图类型检查随工具退休改为下线检查，不移除校园功能测试。
- 后台与 Godot 脚本验收见 `production/VERIFICATION.md`。解锁后已实看校园、七图导航和手机，实点离线接口保存/应用及关闭恢复，均通过。全局 485 项通过，资源与导航 30 项再通过。本批已同步开发分支 `1ff582c`，未合并 main。

## 第四批 A：校园内容独立与旧校园档迁移

- 校园默认注册器改用 `items/campus_catalog.json` 的 9 种物品，不再加载旧 `catalog`、`uses`、`shops`、`placements`、旧通路/区域及旧人口生成配置，共 7 份旧文档。3 个校园商店仍由校园经济配置负责，价格、重量、效果和行为不改。
- 生产校园在上述 7 文件完全不存在的隔离目录下仍可正常初始化。旧文件暂仅供仍在退休中的旧运行时及其历史测试使用；下一批一起删除，不是重新接回校园。
- 内容版本从 `fbd92d2a3d7bef67` 变为 `382ffb9aa84a36d0`。迁移白名单记录源/目标完整内容清单；只接受已知版本、已知清单和已具有完整资源系统的校园档。未知内容变化、原始城镇档、坏校验和不能绕过拒绝机制。
- 迁移只在内存更换内容版本和清单，不改变人物、库存、钱、生命、任务、事件、日程、随机序列及幂等记录，不写原档。正常再次保存仍保留原档为上一份备份；后续新增内容需独立兼容策略，不承诺任意版本自动迁移。
- 验收使用改动前真正生成的旧校园档，除内容身份外全状态及 RNG 一致；Godot 手机读取/确认/场景恢复已实测。完整回归记录见 `production/VERIFICATION.md`。

## 剩余下线顺序

1. 服务启动解耦已完成，保留独立新进程验收，防止后续又引入旧世界初始化。
2. 校园默认入口、API 设置与存档目录兼容已完成。
3. 旧客户端与地图工具已上传，七图、模块化人物、相机与共用设置面板保留。
4. 校园实际使用的数据与严格校园档迁移已实现；完成第四批 A 验收上传后，与旧运行时一起移除剩余纯城镇文件。校园清单已不含这些文件，物理删除不会再次改变校园内容哈希。
5. 清理不再可达的旧 `runtime`、系统、故事及测试；保留真正被校园调用的共用算法。相应旧测试是随退休功能删除，校园交易、资源、任务、LLM、卡牌、路径与存档测试必须保留并回归。
6. 每批做全局、Godot 实际运行及对应多日检查后上传开发分支。全部解除后，才能声称“旧城镇已完全下线”，再继续原路线第 3 步 UI 统一。

目前前三批已上传；第四批 A 已实现内容独立与严格迁移，旧 Python 兼容测试及纯城镇文件仍待退休。这份清单是优先前置，不新增玩法需求。
