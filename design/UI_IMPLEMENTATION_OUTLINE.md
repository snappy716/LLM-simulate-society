# 校园 Demo UI 设计与实施大纲

更新日期：2026-09-11。

用途：作为后续 UI 设计、开发、联调和验收的参考基线。本文保留完整设计范围，并记录可运行第一版的当前状态；实际完成项、截图和剩余边界见 [UI03–UI18 验收](../production/UI03_UI18_ACCEPTANCE.md)，不能将范围清单视为所有细节均已定制完成。

## 1. 目标与使用方式

目前基础可玩系统已交付至实施路线第 12 步。UI 工作重点是把现有功能整理成完整、清楚、可操作的游戏体验，让玩家感受到：NPC 有自己的生活，任务会被其他人推进，自己的行为会改变人物和校园。

- 总体系统进度仍以 [IMPLEMENTATION_ROADMAP.md](IMPLEMENTATION_ROADMAP.md) 为准，不重编号、不撤销已有基础验收。
- 本文规定后续 UI 的范围、分批顺序和验收要求；既有实现记录见 [UI_FOUNDATION.md](UI_FOUNDATION.md) 与 [UI_RUNTIME_OPTIMIZATION.md](UI_RUNTIME_OPTIMIZATION.md)。历史截图和旧入口数量不代表当前最新布局。
- 开始每批工作前核对实际代码和接口，区分界面缺口与后端前置条件。已有面板优先整理复用，不另建重复系统。
- 新增展示需要新数据时，先补权威投影与测试；不能由 UI 或 LLM 编造状态。涉及未确定的玩法变化时停下询问。
- 本文中的“需要补充”是待实施范围，不表示已有；完成一批后记录日期、代码、验证和遗留问题，再勾选工作包。

状态说明：**已有待优化**表示已有操作入口但需整理体验；**需要补充**表示新增展示或交互，可能涉及接口；**后续预留**表示主线或正式发行阶段再完成。

### 当前优先批 用户指定的常驻 HUD

2026-09-11 补充：场景层由美术协作者完成，当前先做常驻信息层。以下位置与内容约束优先于本文 UI03 和此前通用布局建议；其他模块仍保留后续范围，不自动加入本批 HUD。

| 位置 | 本批允许内容 | 行为 |
| --- | --- | --- |
| 左上角 | 当前地点文字、日期、星期、时段 | 始终显示，紧凑布局 |
| 左上角下方 | 当前追踪事项、近期约定 | 默认只展开一件，可折叠 |
| 左下角 | 手机入口 | 始终显示；本时段首次收到新消息时短暂震动一次，此时段后续消息不再震动 |
| 右上角 | 地图、队友、人物状态与物品合并入口、卡牌库、关系 | 组成同一组，共五个入口；关系入口呈现交友和恋爱系统 |

- 上述区域不添加其他常驻内容；本批不额外挂生命/专注条、污染数值、行动计数、通知横幅、调试说明或结束时段按钮。人物状态与物品信息在合并入口打开后查看。
- 手机震动是图标的短暂视觉反馈，不是持续动画，也不是每条消息震动。按游戏日期与时段标识去重，不能因关闭手机或重建 HUD 重复触发；读取旧消息不算收到新消息。跨时段只允许新消息触发下一次，不因遗留未读自动震动。
- 合并入口内部保持人物状态与物品可分别访问，不因此合并后端数据模型。不得为隐藏旧 HUD 控件而删除原有功能；推进时间等操作应保留可达入口，具体入口迁移在实际实现前核对。
- 既有极简约束：实心功能轮廓，冰白主体 `#dff5ff`、亮蓝点缀 `#1888ff`，不复制参考游戏资产；去掉外侧星轨、底座、框线与复杂装饰，手机保持空屏。2026-09-11 纸质剪影试稿：六枚分别为手机、折叠导览图、叠放证件、人物口袋帆布包、书签式指令卡和双照片；不再使用月相切口，待审阅后扩展内部应用图标。
- Godot 当前六枚方向稿使用 `game/assets/ui/campus_paper/` 的透明 SVG，原 `campus_moon/` 图标保留。以证件、帆布包、导览图、书签指令卡和照片的双色块面与叠放替代通用符号；不是整套手机图标重做。64 px 源画布、30 px 图形上限、46 px 热区；常态不透明亮白/清亮蓝，沿图形轮廓加细暗边，悬停/键盘焦点在 0.16 秒内渐亮，保留中文提示。日期和文字不再降低透明度，辅以细描边。保留紧凑布局，不增加面板、底框或发光装饰；不以融入场景为由牺牲可读性。
- 文字层级：地点 18 px，日期/星期/时段 13 px，以中点分隔；当前为亮白文字、细暗边，不再透明压暗。右上组同步收紧，保持同一基线和既定入口顺序。不会为追求融合而隐藏关键功能或缩小点击区至图形大小。
- 入口对应：地图→已有校园地图；队友→行动小队；人物与物品→人物状态/随身物品两个页签；卡牌库→角色八张牌组配置和本人可用牌的费用/效果；关系→交友恋爱协议与相处/约会。使用原权威数据与操作，不新增一套业务规则。
- 时间操作迁至手机顶部“时间与镜头”：保留结束时段、进出里世界和 1×/2×/3× 镜头操作。健康与行动信息移至内部状态页。原常驻调试文字、相机按钮与状态面板不再显示；调试场景保留原有测试节点。
- 追踪来自玩家已接的未完成委托与已确认约定；默认只展开一件，可切换、折叠和打开原详情。没有真实事项时不编造提示。日期沿用“第 N 天”，第一天星期一，与后端周循环一致；尚未指定的公历开学日期不虚构。

## 2. 共同原则

1. 延续写实校园风格。表世界与里世界可使用不同色调和细节，但保持同一套操作逻辑。现有七张场景图继续使用，临时图标和字体必须有清楚的许可来源。
2. 保留手机作为生活入口，复杂页面可展开为宽屏面板。正式战斗使用专用界面，不继续挤在手机表单里。
3. NPC 平常不显示调试状态文字。人物信息、位置、日志与动态均遵守玩家知情权限，不提供全校实时定位或全知内心。
4. 区分计划、承诺、实际执行和最终结果。聊天同意不等于正式预约，战斗胜利不必然等于任务完成。
5. 聊天、购物、吃饭、普通移动不扣主要行动；界面清楚展示真正消耗行动的操作，不新增分钟级时间管理。
6. Godot 展示权威数据并发送命令；模拟规则、伤害、费用、库存、任务锁和关系成立仍由后端校验结算。
7. 玩家即时 LLM 聊天不新增硬次数上限。开发测试控制付费调用，区分本地服务连接与模型 API 可用性，不泄露个人密钥。
8. 允许多位正式伴侣，各段关系独立确认与结束，不在 UI 中偷偷增加排他规则。
9. UI 验收不等于最终美术、长期故事质量或 Windows 发行验收。受控测试场景不能标成自然涌现结果。

## 3. 整体界面结构

| 层级 | 包含内容 | 职责 |
| --- | --- | --- |
| 场景层 | 校园、人物、物件、建筑与区域入口 | 自然呈现可见世界及交互目标 |
| 常驻信息层 | HUD、目标提示、少量通知 | 保留必要信息，不遮挡校园 |
| 手机与管理层 | 通讯、论坛、日程、人物、物品、生活 | 查询、规划和发起操作 |
| 专用玩法层 | 当面对话、调查现场、部署、正式战斗 | 为复杂玩法提供独立空间 |
| 系统层 | 开始游戏、存读档、设置、API、过场、异常提示 | 保证进入、恢复和继续游戏 |

## 4. 完整 UI 模块大纲

### UI01 公共视觉与交互规范

状态：公共主题与组件底座已实现，当前验收见 `production/UI01_UI02_ACCEPTANCE.md`；不代表其余业务页面完成专项视觉重排。

- 统一字体、字号、配色、图标、间距、面板和信息层级。
- 统一按钮、分页、搜索、筛选、滚动、弹窗和提示组件。
- 统一返回、关闭、确认、取消及键盘操作，区分主要、次要和危险操作。
- 覆盖加载中、无内容、不可操作、失败、结果尚未确认等状态。
- 对禁用操作解释原因；不能把未实现功能伪装成可用按钮。

### UI02 游戏开始与系统菜单

状态：标题、新游戏、继续、存读档、设置入口、引导与暂停菜单已实现，当前验收见 `production/UI01_UI02_ACCEPTANCE.md`。

- 标题页：继续游戏、新游戏、读取存档、设置、退出。
- 新游戏页面仅暴露当前已支持的角色与世界生成选项，不临时增加角色创建玩法。
- 初次进入的简短操作引导与暂停菜单。
- 存档列表展示日期、地点、已有时长信息和版本兼容情况；缺少的数据先补接口或不展示。
- 覆盖存档、返回标题和退出游戏的确认提示。

### UI03 校园探索 HUD

状态：用户确认的极简 HUD 已完成，本轮全局回归通过；不新增其他常驻角落内容。

- 日期、星期、当前时段、地点、表里世界状态、主要行动次数。
- 生命、专注、污染等必要状态。
- 当前追踪目标、近期约定和附近可交互对象提示。
- 手机未读消息、邀约与紧急事件提醒。
- 结束时段、进入或离开里世界的入口和不可用原因。
- 普通游玩隐藏 NPC 调试文字，检查功能按需展开。

### UI04 地图与场景交互

状态：第一版已补齐地图预览、道路/入口、开放状态、公开活动与已知任务约定；选中与前往分开。

- 校园区域地图、当前位置、区域连接、可进入建筑与室内地点。
- 地点详情：用途、开放时段、当前可参加活动。
- 已知任务、约定、夜间区域和已发现异常的标记。
- 场景出口、楼梯、门口等自然交互提示。
- NPC 位置仅展示玩家能够知道的信息。精确碰撞、遮挡与动画另属场景实现，不因添加地图标记而视为完成。

### UI05 手机首页与通知中心

状态：第一版已整合通知摘要、功能检索与原事项跳转，不额外维护游戏状态。

- 常用功能入口、分类与搜索。
- 未读消息、待回应邀请、即将开始的活动与今日重点事项。
- 通知直接跳到对应人物、任务或日程。
- 重要通知与普通动态分级，避免持续弹窗。
- 通知只做入口和摘要，不维护第二份任务或关系状态。

### UI06 当面对话与手机通讯

状态：第一版已补齐联系人搜索、逐人草稿、通讯内部导航及请求入口，沿用原对话与提交保护。

- 当面对话：人物信息、对话记录、输入区、可用互动。
- 手机通讯：联系人列表、搜索、聊天记录、未读状态。
- 添加联系方式、邀请见面、提出协作的入口。
- NPC 主动消息及需要玩家回应的请求。
- 回复中、连接失败、草稿保留与安全重试反馈。
- 将聊天提议导向正式邀约或任务操作，明确区分表达意愿和已建立的承诺。

### UI07 NPC 档案与日程日志

状态：已有权威档案/日志保留并回归；新增人物日志、当面交谈、联系与深度交互快捷定位。

- 概况：学院、职业、社团、已知特征、近期情况与打算。
- 按日期展开的日程日志、重要经历和共同经历。
- 与玩家的关系及可用互动，提供消息、邀约、交易和组队快捷入口。
- 明确区分实际活动、对方告知的计划、听闻消息和未证实推测。
- 日志来自真实事件并按知情权限过滤，不把未来计划写成已完成活动。

### UI08 交友恋爱与深度交互

状态：第一版已增加好友/恋人/待回应总览，逐段关系管理仍需双方真实同意。

- 好友和恋爱关系列表、共同经历、待回应请求与近期约会。
- 发起相处、约会、关系确认、结束关系，以及接受、拒绝、取消、失约结果。
- 在基础 20 名 LLM NPC 之外，为符合条件的好友开启额外深度交互。
- 展示开启条件、当前状态及可能增加 API 用量的说明。
- 多位正式伴侣独立呈现，不替玩家自动同意，不凭高好感直接宣告关系成立。

### UI09 日程与行动安排

状态：第一版已有未来七天四时段日历、本人确认安排和对应管理跳转。

- 今日四时段安排和一周日历。
- 已确认约定、报名活动、出击预约及地点、参与者、行动成本。
- 时间冲突、资格不足、错过活动的提示。
- 前往地点、取消安排和实际参加入口。
- 结束时段前提醒重要未处理约定，不自动代替玩家参加。

### UI10 双层论坛与任务管理

状态：真实双论坛行为保持并回归，增加校园动态和原任务互跳，不造浏览量或成交记录。

- 表世界与里世界论坛，帖子、委托、校园动态的分类筛选。
- 任务详情：来源、目标、地点、条件、奖励、截止时间。
- 真实的查看、考虑、接取、执行及结算状态。
- 我的委托、发布的求助和参与的协作。
- 被别人接走、完成、失败和到期后的记录。
- 任务追踪，以及人物、地点和线索跳转。
- 列表在实际变化时更新，不以假浏览量、假接单或通知制造繁荣。

### UI11 校园动态与事件回顾

状态：第一版已接入今日/昨日/历史/通知筛选与分页；记录类型和知情边界见 `../production/UI03_UI18_ACCEPTANCE.md`。

- 今天发生的可知事件，以及 NPC 已完成的委托。
- 玩家关注的人物变化和短缺、纠纷、异常的发展。
- 玩家参与或未参与的事件结果及关联记录。
- 从事件追溯任务、人物和公开日志；只显示有证据的关联，不自动补因果。
- 可作为论坛内的“校园动态”分页，不强制增加独立应用。
- 全部摘要基于真实事件和知情范围，LLM 不为热闹编造结果。

### UI12 课程与校园生活

状态：第一版已增加课程活动、本人参与、公开结果快捷导航，现有生活操作回归通过。

- 课程列表、单元进度和资格解锁。
- 兼职岗位、班次、条件、报酬与实际完成记录。
- 社团列表、成员、活动时间、贡献、晋升与公共资源使用确认。
- 比赛和节庆的报名、到场、个人表现和公开结果。
- 医院服务、治疗条件、费用与结果，以及休息恢复预览。
- 复用“列表—详情—报名—到场—结果”的活动组件，报名不等于完成。

### UI13 背包与交易

状态：第一版已有物品搜索/分类与操作定位，出售与商店库存明确区分；全部原有结算行为保留。

- 背包分类、搜索、数量和详情。
- 使用、赠送、丢弃、拾取、装备、卸下及必要的对象选择。
- 商品价格、库存、营业状态与购买结果。
- 私人报价、接受、拒绝和成交记录。
- 钱包与收支明细，使用前的真实效果和条件说明。
- 明确分开购买商品与管理已有物品；不由客户端重复推算成交和物品效果。

### UI14 调查与知识成长

状态：第一版已有线索搜索、调查/分享/推测导航、动态到原线索跳转及卡牌图鉴。

- 线索列表、来源、取得时间、可信程度和知情权限。
- 现场可交互对象、搜索结果、询问、分享、保密和笔记。
- 已证实事实与人物推测的区别。
- 阅读、课程、案例理解、实践、反思与洞察进度。
- 基础属性、学院能力、已解锁卡牌及成长来源和实际效果。
- 关系图或线索板后续再补，不自动揭示玩家尚未发现的真相。

### UI15 队伍与卡组管理

状态：第一版已有小队/邀人/出击快捷导航、个人牌组配置与卡牌图鉴；资格/集合/八张牌组规则不变。

- 邀请成员、对方意愿、同行承诺和集合情况。
- 队员属性、状态、能力和可用卡牌。
- 出战成员选择，人物卡、技能卡、指令卡的清晰区分。
- 可用卡牌浏览、来源、效果及当前规则支持的卡组配置。
- 战前资源、伤势和行动预算检查。
- 邀请成功、已经到场、可以上场分别显示；不把 UI 改造变成未经确认的新构筑规则。

### UI16 卡牌战斗专用界面

状态：第一版独立战场已实现：三排人物牌部署、手牌、指令点、生命条、敌方意图、合法目标与确认出牌。细项指令复用下方控制区，专用插画与动作后续定制。

1. 战前准备：任务目标、成员、前中后三排部署、确认开战。
2. 战斗主界面：双方角色卡、生命状态、敌人意图、手牌、共享费用和回合操作。
3. 操作反馈：选牌、合法目标、高亮、消耗与效果预览、伤害和状态变化。预览须由权威规则提供，不泄露隐藏信息。
4. 战斗结算：胜利、撤退、败北、消耗、伤势、知识成长和任务进度。

战斗胜利与现场任务完成分开呈现；胜利后仍需处置现场时，清楚提示下一步。表现可以先用简要素材，动作和定制美术由后续协作替换。

### UI17 跨日过场与次日简报

状态：过夜等待与恢复已回归，增加减少动画选项；次日回顾通过手机“昨日回顾”查看，不额外强制等待。

- 入夜至清晨的过场、真实处理阶段与等待提示。
- 网络慢、超时和部分 NPC 回退规则的状态说明。
- 新一天的约定、消息与可知校园变化摘要。
- 允许展开细节，不强制阅读全部报告。
- 不伪造进度百分比，不公开 NPC 内部思考；日内不增加全量 LLM 重规划。

### UI18 设置与技术状态

状态：第一版已有显示/帧率/默认正文号/音量/减弱动画、明确收费确认的 API 短测试及分开计量；自定义键位与更细画质预设尚不提供。Windows 原生验收单列。

- 画面、窗口、声音、字体和操作设置。
- 通用 API 地址、模型、密钥与连接测试；测试请求需明确提示可能消耗额度。
- 密钥遮罩和日志脱敏。
- 自动规划与即时聊天的配置区别，请求状态、Token 用量和统计范围。
- 超时、余额不足、非法响应、规则回退、服务断线和恢复提示。
- 存档兼容说明；本地模拟连接与 LLM 可用性分开呈现。
- 未有可靠单价时不展示伪精确费用；实际请求结果未确认时不盲目重发变更操作。

### UI19 主线专用界面

状态：后续预留，不在当前 UI 优化中提前制作剧情。

- 阶段目标与七天 DDL。
- 导师消息和推荐书目。
- 主线调查进度、重要选择、阶段结算与结局回顾。
- 现阶段仅预留扩展位置与接口，随实施路线第 13 步接入。

## 5. 建议实施顺序

以下勾选表示本轮可运行界面第一版已接入并完成 Mac Godot 验收，不表示商业定制美术、精细动作或 Windows 原生验收已完成。详细范围见 `../production/UI03_UI18_ACCEPTANCE.md`。

- [x] U1 公共规范、导航与 HUD：UI01、UI03，统一各页返回、焦点与操作反馈，检查地图入口 UI04。
  - [x] U1a 用户确认的极简常驻 HUD、六枚图标、直接功能入口与时间操作迁移已实现；专项后台 Godot 与实际渲染截图检查通过。全局回归记录见本批验收文件，不代表所有内部页面视觉重做完成。
  - [x] U1b UI01 公共规范：亮白/清亮蓝主题、主次危险按钮、搜索/筛选/分页、提示、确认取消与焦点约定；本批全量与专项验收通过，规范见 `UI01_PUBLIC_SPEC.md`。其余业务页专项重排继续按 U2～U7 推进。
- [x] U2 手机、通讯、NPC 档案与日志：UI05～UI07，让玩家理解人物并建立联系。
- [x] U3 论坛、任务、校园动态与日程：UI09～UI11，优先表现 NPC 自主行动和可追溯后果。
- [x] U4 队伍、卡组与战斗专用界面：UI15～UI16，将已实现规则转为清楚可玩的卡牌体验。
- [x] U5 地图信息、背包交易、调查成长、校园生活：补齐 UI04、UI12～UI14，复用共同组件。
- [x] U6 关系总览、跨日简报、设置与启动入口：UI08、UI17～UI18、UI02。既有关系功能在此前批次必须保持可用。
  - [x] 用户此前指定提前完成 UI02：真正标题场景、暂停菜单、新游戏权威重建、最近兼容档继续、既有存档/API 设置整合；本轮已继续完成 U6 其余界面第一版，分别记录验收。
- [ ] U7 全界面联调与 Windows 验收：新旧存档、窗口适配、键盘、长文本、故障恢复和真实操作链路。
  - [x] Mac：69 项 Godot 全局流程及最终增量界面专项已验收。
  - [ ] Windows：原生显示模式、字体/输入法、实际 API 网络及打包后的人工检查。
- [ ] 后续 UI19 主线界面：等待第 13 步剧情工作启动，不因本清单自动推进。

玩家感知优先项为人物日志、动态论坛、事件回顾，以及独立卡牌战斗界面；公共组件先行，避免这些页面各自重复搭建。

## 6. 每批交付与验收

### 开始前

- 核对对应面板、后端投影、请求与事件，记录复用范围和缺失前置条件。
- 先明确页面入口、信息层级和主要操作，再实现组件；此大纲不规定所有按钮的最终尺寸。
- 保留用户的无关改动与私人文件，不因 UI 优化改动人物自主决策、行动计费或关系规则。

### 必测状态

- 正常操作、空列表、长文本、窄窗口和不同缩放。
- 等待、成功、业务拒绝、断线、结果未确认、无权限与资格失效。
- 返回、关闭、焦点、键盘可达性和输入草稿保留。
- 任务在查看中被接走、库存变化、邀约冲突等过期状态，提交时以服务端最新校验为准。
- 页面之间状态一致，UI 不产生额外资源、重复结算、免费行动扣时或隐藏信息泄漏。

### 完成条件

- 专项测试及与风险相称的全局 Python/Godot 回归通过，不能只凭控件存在勾选。
- 在 Godot 实际运行中检查布局和操作结果，关键页面保留带版本说明的截图。默认后台运行，不抢占前台；若当前环境不能完成视觉验证，应明确记录，不冒称通过。
- UI 变化不默认触发付费 API 多日重测；确需真实调用时使用有限样本，记录 Token 和验证范围。
- 记录完成内容、验证结果和剩余边界，才更新本节工作包状态。
- 后续实现批次沿用项目约定：测试通过后上传开发分支并核验，不合并 main。文档创建、代码实现、验证及上传分别记录，不将规划条目当成已完成功能。
- Windows 字体、输入、缩放和干净机器运行需实际验收，不能以 Mac 上通过替代。

## 7. 批次记录

| 日期 | 工作包 | 内容与证据 | 状态 |
| --- | --- | --- | --- |
| 2026-09-11 | 大纲落盘 | 保存已讨论的 19 类 UI 范围、U1～U7 顺序及验收要求，并在总路线加入引用 | 规划文档已建立，未开始本轮 UI 改造 |
| 2026-09-11 | U1a 常驻 HUD | 六枚原创 SVG、四角布局、内部功能入口、消息去重、室内外共用；`game/tools/test_campus_minimal_hud.gd`、`game/tools/capture_campus_minimal_hud.gd` | 已实现并通过专项；完整验收与上传以 `production/UI_MINIMAL_HUD_ACCEPTANCE.md` 为准 |
| 2026-09-11 | U1a 场景融合打磨 | 缩小图形保留热区、雾蓝材质、柔阴影、渐亮反馈、轻量文字排版；七张既有地图实际渲染检查 | 已实现；本轮测试与边界见 `production/UI_SCENE_INK_ACCEPTANCE.md`，不代表内部功能页完成视觉改版 |
| 2026-09-11 | UI01 / UI02 | 亮白清亮蓝公共主题与组件、标题、新游戏、暂停、引导、存读档、设置接续；`production/UI01_UI02_ACCEPTANCE.md` | 95 模块 / 910 项 Python、67 条 Godot 与追加专项通过，已检查后台实机截图；Windows 原生验收另计 |
| 2026-09-11 | UI03–UI18 统一版 | 地图预览、通讯草稿与检索、关系分类、日历、动态溯源、物品/线索检索、队伍卡库、独立战场、跨日回顾及本地设置；`production/UI03_UI18_ACCEPTANCE.md` | 96 模块 / 914 项 Python、69 条 Godot 全局及最终专项通过，后台截图已核对；Windows 原生验收、定制美术及 UI19 仍未完成 |
| 2026-09-11 | UI 图像美术第一批 | 1 张生成月湖标题图、16 个手机图标、4 个战斗语义图标、2 个卡面边饰；`production/UI_ART_BATCH_01_ACCEPTANCE.md` | 已接入标题、手机、夜战和卡库；素材来源、真实截图与最终验收结果以该页为准。未替换七张校园地图或制作角色专属立绘 |
| 2026-09-11 | UI 图像美术第二批 | 雁栖湖校区建筑语言参考、3 张原创写实青春校园 UI 图片、图标扩至 65 枚、蓝色面板提亮及语义接入；`production/UI_ART_BATCH_02_ACCEPTANCE.md` | 96 模块 / 918 项 Python、70 条 Godot 全局及最终 4 项专项通过；后台实渲染截图已核对。七张可行走地图不变，未宣称指定生图型号，Windows 原生验收仍待完成 |
| 2026-09-11 | 六枚校园纸质剪影试稿 | 六个常驻入口改用校园物件、双色块面与叠放；`production/UI_PAPER_ICONS_ACCEPTANCE.md` | 96 模块 / 919 项 Python、70 条 Godot 全局及新增断言后的 HUD 专项通过；实尺寸对照、场景和焦点截图已检查。旧图标保留，手机内部 65 枚尚未扩展，Windows 原生验收仍待完成 |
