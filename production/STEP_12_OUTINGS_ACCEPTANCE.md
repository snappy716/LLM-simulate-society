# 第 12 步：自愿相处与约会验收

2026-09-10。开发分支 `codex/campus-demo-architecture`。本批完整本地验收通过；仅向该开发分支发布，不合并 main。文档中的测试通过不等于远端已上传，发布结果另以远端提交核验。

## 已实现并验收的范围

- 五个正式邀请/回应/到场命令；双方自愿、现有联系人、日程冲突、冷却、取消和失约。
- 普通相处与明确约会意图分开，不能通过一次接受直接确立恋爱关系。
- NPC 实际沿路到场、玩家明确到场确认、双方预算原子结算、真实共享经历和私有收据。
- 既有每日规划中的可选邀请，规则与模型共用；不增加日内规划请求，不修改玩家聊天额度。
- 共享经历可作为原有自愿锚点的候选来源，仍需当前同意和知识。
- 手机「日程与约定」新增完整操作入口和真实反馈，复用主题、HTTP 事务和存档。

设计边界：`design/CAMPUS_OUTINGS_RUNTIME.md`。自主候选暂用普通相处，明确约会走同一协议；不宣称完整伴侣剧情、恋爱偏好系统或真实模型自然社交质量已验收。

## 已获得的定向证据

- 新功能最初 10 项定向测试通过，含假模型在 20 次跨日规划中选中并实际完成共同活动、隐私、双方真实成本、保存恢复、失败/拒绝等。
- 57 项关联预检通过；随后新增的「预留预算与另一方耗尽时不能单边扣费」测试也单独通过。
- 后台 Godot 3 条定向流程通过：`outings`、`agenda_flow`、`life`。新流程经实际手机按钮/HTTP 接受邀请，正常推进时段后 NPC 到场，玩家确认后扣费；检查三个窗口尺寸和本人收据可见性。
- 以上是全局验收前的证据，不替代最终同一代码状态的完整回归。
- 使用上一批 Godot 实际存档验证新运行时恢复：原文件 SHA-256 `133732dae76bb61df6dd9a01bc56d501c0f33240888bde8ef925077d1eb6bf19`，内容版本仍为 `74314dd2ea32e235`。除既有读档保护策略提升世界 revision 外，整个世界和 RNG 完全一致，源文件未修改、没有补造相处记录。脚本及结果在本批验证目录。

## 最终完整验收

修复后冻结同一源码重跑，两套测试均取得真实进程退出码 0：

- Python：89 个模块、844 项测试全部通过。汇总 `production/validation/step12-outings/python-final.log`，逐模块日志 `python-final-modules.tar.gz`。包括新功能 11 项、旧档/契约、日程、课程兼职、交易、战斗、LLM 协议和完整因果对照。
- Godot：全部 57 条流程已通过，进程退出码 0，含新相处界面及全部既有战斗/调查/手机/存档/课程/兼职流程。汇总 `production/validation/step12-outings/godot-final.log`，逐流程 114 份日志在 `godot-final-flows.tar.gz`。

首轮因复现最小世界视图缺少校园地点而主动中止（两进程均 143）。修复后原有契约加新功能共 16 项测试通过，再重跑上述完整验收；不把首轮结果当成最终通过。故障和初轮日志保留在 `production/validation/step12-outings/`。

本批所有测试为离线/假提供方，付费 API 调用为 0。不是 Windows 发布验收，也不证明真实 LLM 的自然约会体验。上传完成后，下一批为医疗与安保服务；第 12 步还需比赛与节庆，不推进主线。
