# 第 12 步第三批：课程进度与真实兼职报酬

状态：修复后完整 88 模块 / 833 项 Python、56 条后台 Godot 流程全部通过，退出码均为 0。本批验收完成，发布开发分支，不合并 main；是否上传以远端提交核验为准。运行边界见 `design/CAMPUS_STUDY_WORK_RUNTIME.md`。

## 专项证据

- 课程/兼职、原公开活动、内容注册、历史内容链、好友、夜相实地任务与设施兼容组合：70 项通过，退出码 0，见 `compatibility.log`；完整回归仍需另行等待，不以此替代。
- 两门三单元课程、两个有报酬班次沿同一报名与到场账本执行；完成数据素养才能接盘点班，不接受客户端伪造资格或工资。
- 11 项新增专项覆盖逐次到课、课程完成和资格解锁、知识主题且不赠送异常案例/应用/反思、取消/错过无进度、真实金钱守恒、重复拒绝、付款不足原子失败、NPC 与玩家争同一岗位、损坏定义/凭据、无人参与三日、替身每日规划选择、自动夜相入场尊重已确认活动，以及旧档/当前档恢复。
- 三日无人干预实际出现 NPC 上课与兼职完成记录；全程无 API。模型替身可在原一次每日规划中选择课程，20 名每日规划且均在早晨，不证明真实模型的内容偏好。
- Godot `study` 使用课程平台按钮进入活动界面，真实 HTTP 报名/到场，课程由 0/3 到 1/3、扣一次主要行动、时钟不变，检查三尺寸宽度。
- Godot `work` 夹具只用真实命令先报名并沿路到食堂，随后由实际 UI 参加；余额增加恰为 12，记录付款方，重复领取失败。没有注入工资或作假出勤。

## 发现与修正

- 内容引用校验需要同时支持完整与局部注册表；没有经济配置的局部测试不能因直接读取完整配置而崩溃。
- 原生活列表测试改为根据已加载定义核验三天场次数，不再把首批三个活动的九场写成永久上限。
- 兼职 Godot 专项初版错误地从不含玩家的 NPC 投影取余额；改用已有 `snapshot.economy.balance`，课程与兼职两条重跑均通过。未改变正式转账规则来迎合测试。
- 首轮七日回归在 seed 42、第 5 天晚上复现：`campus_student_003` 早晨确认食堂晚班，但旧自动夜相入场先于活动执行，把他带入夜相，导致表世界活动 `opportunity_unavailable`。修复 `_eligible_night_npcs`：自动入场检查当晚及凌晨已有 life 约定；不覆盖本人已确认的表世界承诺，不限制玩家主动进出。新增报名/取消前后的资格专项通过，原七日完成断言未放宽。专项初版只改真实时钟却未改行动预算的错误夹具已改为只读晚间投影，正式报名/取消仍走真实时钟。
- 修复后原七日行为测试已通过（`entry-fix-initial.log` 中该项为 ok；该次整组因当时旧夹具错误仍是退出码 1，不能当作整组通过）；修正夹具后的独立入场保护测试退出码 0，见 `entry-guard.log`。首轮完整 Godot 56 条通过，但期间有入场逻辑修改，因此另外启动当前代码的完整终验，不把混合时点首轮结果冒充最终版本的通过证据。
- 首轮 Python 已结束并归档：88 个模块运行完，仅原 `test_campus_decisions` 模块失败；由于首轮启动发现 832 项、期间补入一条保护回归，实际模块合计为 833，汇总计数校验也拒绝通过，退出码 1。当前代码冻结后的终验从发现阶段即为 88 模块 / 833 项，不把补测拼接成一次完整通过。

## 实际旧档

复用上一批完整 Godot `save_flow` 的真实 `slot_1.json`，SHA256、迁移边和核验结果见 `production/validation/step12-study-work/old-save-migration.json`。除了新增定义和内容版本，全部旧权威状态与随机数相同；原文件字节未变，当前完整运行时恢复通过，没有给旧档新增已学单元。

日志保留首轮失败与修正；最终完整 Python 成功标记为 `GLOBAL_OK 88 833`，见 `python-final.log`、`python-final-modules.tar.gz`，完整进程退出码 0。Mac 后台 headless 验证不冒充 Windows 干净机器发布验收；本批未调用付费 API。

修复后 Godot 完整终验见 `godot-final.log` 与 `godot-final-flows.tar.gz`（56 条流程、112 份流程日志），没有沿用首轮混合时点日志作为终验。
