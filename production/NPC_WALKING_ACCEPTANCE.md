# NPC 行走节奏验收（2026-09-10）

基线 `c79e4f9`，分支 `codex/campus-demo-architecture`；不合并 main。行为规则见 `design/NPC_WALKING_PRESENTATION.md`。

## 本轮实现

慢速且稳定的个体步速、独立起步延迟、最多四条同时回放的路线、途中节点暂停、保留真实静止居民、到达后不因刷新跳位、暂停界面同步冻结移动、换图/新时段清理旧队列。移动动画随步速放慢，玩家移动和动画默认倍率不变。

只修改 Godot 表现层及测试运行入口，未修改 Python 模拟、LLM 请求或存档格式。地点事件仍来自权威模拟，不新增随意游荡/假 NPC，也不把画面停顿算作新行动；精确道路碰撞与室内摆位仍是后续场景任务。

## 证据与当前状态

- 后台 Godot 导入退出 0。
- 新 `movement_pacing`、原 `navigation_flow` 和完整校园 `collab_flow` 定向复测全部通过、运行器退出 0。覆盖真实时段 HTTP、显式十四人表现层夹具、速度与静止位移、四人并发、界面暂停、终点/节点暂停、清除队列、后端快照不变；不使用付费 API。
- 初轮新测试在固定 1.5 秒采样时尚无人结束起步延迟，断言失败。复查也发现直接使用相邻 ID 的余数会让同批起步过于相近；现使用独立稳定随机器分散节奏，并按首批最早的实际延迟之后采样。仍检查确实有人移动、最多四人移动、位移上限和静止者不动，不以删除断言通过。
- 完整 Python `GLOBAL_OK 91 867`、完整后台 Godot `GODOT_CHECKS_OK 60 flows` 全部通过，顺序运行命令最终退出 0。覆盖新步速流程及既有七图、导航、交互、卡牌战斗、手机、社交、任务、恢复、存读档、过夜并行和医疗入口。日志归档目录 `production/validation/npc-walking/`，包含逐 Python 模块及逐 Godot 流程完整日志压缩包；本轮付费 API 为 0。
- 初次上传因 Mac 锁屏暂停；入口恢复后已通过 GitHub Desktop 推送，远端开发分支 SHA 核验为 `d1c74bb66d9cb30b5ef84bc4296000cee17cf976`，包含本轮步速及其父提交 `c79e4f9` 医疗内容。未合并 main，私人草稿未提交。朋友电脑仍需拉取该分支并重新运行，不能把远端上传等同于对方本地自动更新。

Mac 后台 Godot 不代表 Windows 原生发行验收，本轮没有美术新动作制作或真实 API 延迟验收。
