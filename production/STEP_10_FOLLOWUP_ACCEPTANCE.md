# 第 10 步第六批验收：实际支持后继续发展

2026-09-09。基线 `1ffae41`。只上传开发分支，不合并 main，不纳入私人文件。设计与参数见 `design/CAMPUS_SUPPORT_FOLLOWUP.md`。

新增真实支持的双向关系后果及收据；次日从自己的实际支持产生可拒绝的后续关怀目标，重新听取本人陈述、真实阅读至理解 40、重新约见或确认稳定后结束。保留原已经完成的一次性求助目标，不补造亲历战斗，不赋予直接知识，不改聊天额度或增加日内完整模型判断。过去事件回访指定原事件，避免后来新事件覆盖旧事件的确认。

12 项新增专项与旧目标 16 项联合通过；此前原支持/准备/锚点 32 项通过。Godot 新 `followup` 与原 `anomaly` 专项通过，前者验证真实跨日后的自愿计划，后者增加实际关系反馈检查。测试初次假设 NPC 没有案例，但它确实在正常夜间自行参战；现验证每条案例对应真实参战记录而不是禁止行为。

## 最终验收

程序、UI、契约和测试冻结后，完整 Python `GLOBAL_OK 78 723`、后台 Godot `GODOT_CHECKS_OK 49 flows` 均退出 0。两种子七日正式推进以及独立自然事件链复核也全部退出 0；最终证据归档 `validation/step10-followup/`。

| 七日无人样本 | seed 42 | seed 7 |
| --- | --- | --- |
| 已完成任务 / 实际夜间现场 | 155 / 31 | 159 / 34 |
| 白天支持（包含深入支持） | 5 | 10 |
| 确认关系锚点 / 实际深入支持 | 1 / 1 | 1 / 1 |
| 已稳定人物事件 / 夜间封控 | 1 / 2 | 2 / 3 |
| 已完成支持约见 | 2 | 10 |
| API 调用 / 玩家代办 | 0 / 无 | 0 / 无 |

独立事件链复核只运行 28 次正式时段推进，不注入初始条件或结果。seed 42 中，058 在第 4 天实际支持 066，获得双向关系后果；第 5 天产生后续目标并实际阅读，第 6 天经本人确认共同经历后深入支持。seed 7 中，035 帮助 104 后已有理论 40，因此后续目标直接等待约见，第 6 天完成深入支持，不重复读书。最初核查脚本把两条链都必须再次阅读写成断言，第二个样本因此失败；修正的是证据核查假设，未修改游戏规则或提高知识。修正版验证真实支持来源、合法后续目标、足够知识和实际锚点结果。

两个深入支持均出现在该事件后期：不能据此宣称比普通第三次支持更快，也不能把两个种子当作所有 NPC 或 LLM 故事质量的保证。其他目标仍可能在学习、等待、受阻或得知稳定后结束。

受控夹具只提供首次支持的知识/信任等初始条件及保护职责边界。支持、双向关系变化、过夜、新目标、阅读、询问和支持结果都走实际处理器；没有直接改时钟或注入完成记录。原 NPC 合法夜战等行为仍可发生。自然样本没有初始条件注入、没有玩家代办，不调用 API。

第 10 步尚需战斗洞察、混合路线完整反馈与不同后果；第 11～12 步整体未完成，主线不实施。Godot 后台 headless，不抢前台；Windows 原生发布未验收。
