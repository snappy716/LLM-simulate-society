# 第 12 步：医疗服务验收

2026-09-10，开发分支 `codex/campus-demo-architecture`，基线 `0721284`。不合并 main，不推进主线。

实现边界见 `design/CAMPUS_MEDICAL_RUNTIME.md`：实际医护值班、有限接诊、真实药房物资/现金、患者生命变化、NPC 可选就诊与主体继续、手机入口及私人记录。

## 定向检查

- 医疗专测 12 项通过；含真实三日跨日运行、日内无新增模型规划、模型可选及拒选、普通 NPC 的伤势/经济/风险偏好差异、隐私、存档和容量账本。
- 既有相关模块加首批医疗测试：47 项通过。
- 后台 Godot `medical` 与既有 `recovery` 两条通过：真实入口移动、HTTP 命令、医护服务、费用/生命/预算、私人回执、重复点击保护和三种窗口尺寸。原有充分休息仍全部恢复。
- 开发中一项存档测试夹具误用了检查点函数签名，已按实际接口修正后重跑通过；未放宽运行时验证。

## 全局验收

最终冻结源码的完整 Python 与后台 Godot 均退出 0：`GLOBAL_OK 91 867`、`GODOT_CHECKS_OK 59 flows`。Godot 运行真实游戏场景、手机按钮与 HTTP 结算，并覆盖原有恢复、交易、战斗、调查、约会及过夜并行流程；本机无前台测试窗口。本子批本地验收通过，发布状态以开发分支远端提交核验为准。付费 API 调用为 0，未读取/改写私人密钥，未改动用户的交付文档和世界观草稿。

首轮全局 Godot 在 `expedition` 三日自主出击夹具的 60 秒启动门槛超时，夹具日志没有断言错误；运行器退出 1 并清理其夹具。随后保持相同代码和超时门槛单独复测 `expedition` 通过、退出 0。没有删除测试或缩短三日样本。最终在完整 Python 结束后单独重跑整套 Godot，减少并发回归资源争用，59 条全部通过，包含原超时的出击流程。单项复测不替代整套成功。

首轮完整 Python 最终退出 1：91 模块中 90 个通过，`test_architecture_contracts` 因新增医疗 JSON 契约遗漏项目要求的根级 `type: object` 失败（两项中一项报错）。已补齐该字段，未改测试或放宽参数验证；将重新执行完整 Python，最终成绩只引用修正后的终态。之前的局部通过不能代替全局成功。

最终验证轮（契约修正后）：架构契约、内核接口契约和医疗共 19 项复测通过。完整 Python 为 `GLOBAL_OK 91 867`，运行器退出 0，完整日志与逐模块日志压缩包为 `python-final.log` / `python-final-modules.tar.gz`。完整 Godot 为 `GODOT_CHECKS_OK 59 flows`，运行器退出 0，完整与逐流程日志为 `godot-final.log` / `godot-final-logs.tar.gz`。失败与修正后日志均保存在 `production/validation/step12-medical/`；不归档私人设置、密钥或用户文件。

新增七日无人干预审计：不注入伤势、不替 NPC 指定就诊计划，每次真实就诊核对患者扣款、药房收款、材料消耗、生命变化、专注/污染和主要行动不变；中途保存恢复并核对完整世界与随机数状态。首轮因 Python 原生类型比较失败：`world.places.south_gate_region.map_cell` 在 JSON 中由 tuple 转为 list。复核已确认仅数组表示不同，全部字段值和 RNG 相同；审计仅统一 tuple/list 表示，不丢弃任何字段、不修改运行时代码。首轮日志保留。

两个种子各 28 时段的重跑均完成（`seven-days.log` 中两个 `MEDICAL_SEVEN_DAY_AUDIT_OK`）：

| 种子 | 自主就诊次数 / 不同患者 | 实际医护班次 | 患者向药房转账 | 绷带 / 消毒液消耗 | 生命恢复 | 就诊后主要活动完成 |
| --- | --- | --- | --- | --- | --- | --- |
| 42 | 2 / 2 | 157 | 76 | 2 / 2 | 88 | 2 / 2 |
| 46 | 4 / 2 | 137 | 152 | 4 / 4 | 180 | 4 / 4 |

两组无失败就诊尝试，中途完整检查点及 RNG 一致，付费调用均为 0。这证明普通 NPC 在这两个样本中可自主就诊并继续主体活动；不代表真实 LLM 的自然就诊率或所有种子都必定就诊。LLM 候选的选择/拒选及落地另由可控提供方专测覆盖。

2026-09-10 用户确认暂缓独立安保服务，保留现有安保 NPC 与巡逻。后续仍须完成比赛/节庆及整体验收，不能将此医疗子批计作第 12 步完成。

本机 Mac 后台 Godot 验收不等于 Windows 原生发布验收；本轮本地模拟模型的快速响应也不等于真实 API 已达到十五秒过夜目标。

发布核验（2026-09-10）：本批提交 `c79e4f9` 已随后续步速提交 `d1c74bb66d9cb30b5ef84bc4296000cee17cf976` 推送并核验远端开发分支。未合并 main；锁屏曾阻止上传，但现已解除该次发布阻碍。
