# 第 12 步第一批：统一已确认约定与本人日程

状态：专项与最终完整 Python/Godot 均通过；2026-09-10 已上传开发分支并核验远端提交 `75afaff29dbb67874e3e9b11af77c4b06112e7ab`，未合并 main。本批是生活机会的前置，不等于课程、兼职、约会、医疗安保、比赛节庆已经完成。

## 实际修改

- `campus_commitments` 只读汇总现有支持、出击及见面账本；过滤未确认、已关闭、过期及已有执行收据的短暂见面。不建立新预约副本、金钱或行动余额。
- 出击预约现在检查每位成员已确认的见面，跨日见面协调也检查双方的出击预约。冲突拒绝不覆写原预订、不花资源；同一支队伍重新评估自身预约不会自锁。支持预约改为复用查询，保留同意、课程职责、主要行动及通行条件。
- 所有 LLM 共用规则上下文增加 `own_confirmed_slots`，只投影本人的类型、日期、时段、费用语义，不带私密案例、约见对象或地点。仍由人物结合性格/需求/目标选择，不添加日内全量规划、聊天调用配额或人为强制日程。
- Godot 手机新增“日程与约定”，显示本人已确认记录并链接原小队/通讯管理页。普通聊天和短暂见面仍免费；预约不冒充完成，也不冒充完整课程表。取消后的画面从权威快照刷新，旧存档无需新账本迁移。

## 验收与口径

8 项新增专项涵盖双向冲突、原子拒绝、同队忽略/他队冲突、已结束过滤、双人收据释放、私密数据隔离、只读与保存恢复；与出击/社交/支持联合 39 项通过。边界记录注入是明确测试夹具，不当成自然 LLM 同意。

新 Godot 流程走真实 HTTP 预约/取消和手机按钮，既有支持流程增加接受/取消后的日程页断言。首轮发现 JSON 日期以小数显示，修正为整数；首页布局测试的旧 15 入口计数改为实际 16，并增加日程页面覆盖，没有删掉三种尺寸、图标、键盘可达、禁止横向溢出和不扣行动的断言。

首轮完整 Python 仅有 UI 目录的旧 15 入口断言失败；修正为 16，相关 10 项复测通过后，重新执行完整 `GLOBAL_OK 85 795`，退出 0。Godot 首轮及中间专项保留旧计数失败；最终重新完整执行 `GODOT_CHECKS_OK 53 flows`，退出 0，无脚本错误。三种尺寸均测试全部 16 个入口、实际页面及导航，不把新入口排除在布局验收之外。

原始日志见 `validation/step12-commitments/`，包含初轮失败及最终整套结果；模块/流程原始日志压缩归档。本批不新增付费调用。上一批真实七日样本证明共用规则和采购执行链，但不能冒充本批新增预约上下文的真实模型效果；本批验证的是确定性的权威规则、接口和界面。测试在 Mac 上后台 headless 执行，不代表 Windows 原生发行验收。
