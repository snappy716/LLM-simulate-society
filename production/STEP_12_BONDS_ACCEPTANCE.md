# 第 12 步：交友与恋爱首批验收

日期：2026-09-10。实现规则见 `../design/CAMPUS_FRIENDSHIP_ROMANCE_RUNTIME.md`。

## 交付范围

- 手机日程中的交友/恋爱面板、本人参与的关系记录与独立请求反馈。
- 双方确认好友或恋人；多位正式伴侣可并存，各段关系独立结束。约会不自动确立恋爱，拒绝不扣好感或行动。
- 复用真实相处来源、原关系数值、私人手机线程与内核命令；无第二套好感、预算或 LLM 客户端。
- 普通相处/明确约会的独立每日候选；规则驱动的晨间关系发起、接受/拒绝及到期。NPC 对 NPC 共用协议，玩家同意不可代办。
- 既有对话和每日规划只附带本人关系事实，没有相关关系时不添加空上下文，未新增 API 请求。

## 验收进度

最终源码完整 Python 回归已通过：`GLOBAL_OK 92 883`，退出 0，无失败模块。新增 16 项专项包括两位伴侣并存、独立结束、拒绝与冷却、异议/需求/情绪敏感度影响、真实 NPC 对 NPC 相处、存读档、重放、隐私、关系结束后的模型上下文及候选区分。日志与逐模块日志包位于 `validation/step12-bonds/`。

最终后台 Godot 整套验收已通过：`GODOT_CHECKS_OK 61 flows`，退出 0，无脚本错误。包括新增关系流程与原有全部 60 条流程；逐流程运行日志单独归档。至此本批本地机制验收通过，上传状态以远端开发分支核验为准，不合并 main。

新增 Godot 流程已在整套最终验收中走真实校园场景和 HTTP：读取两份待回应恋爱请求 → 玩家分别接受 → 两段同时生效 → 独立结束其中一段；时钟和行动余额不变，三种窗口尺寸下控件宽度有效。macOS 后台引擎测试不等于 Windows 原生发行验收。

旧档：读取上一版实际 Godot 保存流程产生的 `slot_1.previous.json`，结果 `PRE_BONDS_GODOT_SAVE_OK`。世界仅按既有规则增加加载修订号，RNG 一致，源文件哈希不变，没有追溯创建关系。本次只归档检查脚本和结果，不上传用户存档。

最终源码的两个自然种子（42、314）各连续推进七天，均为 `BONDS_SEVEN_DAYS_OK`，中途保存恢复与全部关系/相处不变量通过。**两组都没有自然产生长相处或正式关系。**没有注入好感、角色计划或成功回执，不以专项的人工关系基础替代自然结果；首批机制可用，但自然社交节奏仍需校准，不能宣称无人世界已自然形成丰富恋爱故事。全程付费游戏 LLM API 调用为 0。

## 修正与证据解释

- 初轮专项的存档测试使用了错误的辅助函数签名，已改为项目既有的状态 + RNG 保存接口。
- 候选专项最初把已消耗的约会预算带入未来时段，导致合法性检查正确拒绝候选；修正夹具，明确刷新未来时段预算，没有删除预算规则或降低断言。
- 复查发现新逻辑误用了项目不存在的性格字段；已改为原有 `emotional_sensitivity` 并重跑专项。此前中断的全局不算通过，最终全局必须基于修正后源码。
- 又补齐关系结束后的最新事实传递，并按创建日期而非 JSON 键顺序选择同一对人物的最新关系。再次中断的旧全局单独归档，不计为通过；上面的 92/883 是上述全部修正后的完整新一轮。
- 无人测试不注入关系、约会、任务结果或 LLM 回答；自然出现为零时如实报告。专项的高关系初值仅用于协议验收，日期、路程和共同活动回执仍由正式执行生成。

## 未宣称完成

这不是复杂恋爱剧情、嫉妒/排他承诺、取向偏好、自然恋爱丰富度或真实 LLM 恋爱质量验收。首批正式关系判断仍由规则负责；不能把它宣传成新增的 LLM 自主告白。比赛/节庆及第 12 步整体验收仍待后续，不推进主线、不合并 main。
